server {
    listen       80; # default port for HTTP traffic but https is 443
    server_name  localhost;

    location / {
        proxy_set_header X-real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_set_header X-NginX-Proxy true;
        proxy_pass http://node-app:3000;
        proxy_redirect off;

        # root   /usr/share/nginx/html;
        # index  index.html index.htm;
    }

    # error_page   500 502 503 504  /50x.html;
    # location = /50x.html {
    #     root   /usr/share/nginx/html;
    # }
}
#/etc/nginx/conf.d/default.conf  
# // if I want to use it in container, I need to copy this file to the container and then use it as default conf for nginx.
# set it in docker compose volumes: